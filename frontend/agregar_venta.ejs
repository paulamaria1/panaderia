<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>Agregar Venta</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body.container {
      background: #fff8f0;
      color: #5a3e1b;
      min-height: 100vh;
      padding-top: 2rem;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    h1 {
      font-weight: 700;
      font-size: 2rem;
      margin-bottom: 1.5rem;
    }

    label {
      font-weight: 600;
    }

    .form-control {
      background-color: #fff4e6;
      border: 1px solid #d2b48c;
      color: #5a3e1b;
    }

    .form-control:focus {
      border-color: #c69c6d;
      box-shadow: 0 0 0 0.2rem rgba(198, 156, 109, 0.25);
    }

    .btn-success {
      background-color: #7b3f00;
      border-color: #7b3f00;
      color: #fff8f0;
      transition: background-color 0.3s ease;
      margin-right: 0.5rem;
    }

    .btn-success:hover {
      background-color: #a55b00;
      border-color: #a55b00;
      color: white;
    }

    .btn-secondary {
      background-color: #7b3f00;
      border-color: #7b3f00;
      color: #fff8f0;
      transition: background-color 0.3s ease;
    }

    .btn-secondary:hover {
      background-color: #a55b00;
      border-color: #a55b00;
      color: white;
    }

    .card {
      background: #fff4e6;
      border-radius: 10px;
      box-shadow: 0 5px 15px rgba(90, 62, 27, 0.2);
      padding: 2rem;
    }
  </style>
</head>
<body class="container">
  <div class="card mt-4">
    <h1>🧾 Agregar Nueva Venta</h1>
    <form action="/ventas" method="POST">
      <div class="form-group">
        <label for="id_producto">Producto</label>
        <select name="id_producto" id="id_producto" class="form-control" required>
          <option value="" disabled selected>-- Selecciona un producto --</option>
          <% productos.forEach(p => { %>
            <option value="<%= p._id %>"><%= p.nombre_producto %> (Stock: <%= p.cantidad %>)</option>
          <% }) %>
        </select>
      </div>

      <div class="form-group">
        <label for="cantidad_vendida">Cantidad Vendida</label>
        <input type="number" name="cantidad_vendida" id="cantidad_vendida" min="1" class="form-control" required>
      </div>

      <div class="form-group">
        <label for="fecha_venta">Fecha de Venta</label>
        <input type="date" name="fecha_venta" id="fecha_venta" class="form-control" required>
      </div>

      <button type="submit" class="btn btn-success">Guardar Venta</button>
      <a href="/ventas" class="btn btn-secondary">Cancelar</a>
    </form>
  </div>
</body>
</html>
