<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Agregar Proveedor</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <style>
    body.container {
      background: #fff8f0;
      color: #5a3e1b;
      min-height: 100vh;
      padding-top: 2rem;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    h1 {
      font-weight: 700;
      font-size: 2rem;
      margin-bottom: 1.5rem;
    }

    label {
      font-weight: 600;
    }

    .form-control {
      background-color: #fff4e6;
      border: 1px solid #d2b48c;
      color: #5a3e1b;
    }

    .form-control:focus {
      border-color: #c69c6d;
      box-shadow: 0 0 0 0.2rem rgba(198, 156, 109, 0.25);
    }

    .btn-primary {
      background-color: #7b3f00;
      border-color: #7b3f00;
      color: #fff8f0;
      transition: background-color 0.3s ease;
      margin-right: 0.5rem;
    }

    .btn-primary:hover {
      background-color: #a55b00;
      border-color: #a55b00;
      color: white;
    }

    .btn-secondary {
      background-color: #7b3f00;
      border-color: #7b3f00;
      color: #fff8f0;
      transition: background-color 0.3s ease;
    }

    .btn-secondary:hover {
      background-color: #a55b00;
      border-color: #a55b00;
      color: white;
    }

    .card {
      background: #fff4e6;
      border-radius: 10px;
      box-shadow: 0 5px 15px rgba(90, 62, 27, 0.2);
      padding: 2rem;
    }
  </style>
</head>
<body class="container">
  <div class="card mt-4">
    <h1>➕ Agregar Nuevo Proveedor</h1>
    <form action="/proveedores" method="POST">
      <div class="form-group">
        <label>Nombre</label>
        <input type="text" name="nombre_proveedor" class="form-control" placeholder="Nombre" required>
      </div>
      <div class="form-group">
        <label>Teléfono</label>
        <input type="text" name="telefono" class="form-control" placeholder="Teléfono" required>
      </div>
      <div class="form-group">
        <label>Dirección</label>
        <input type="text" name="direccion" class="form-control" placeholder="Dirección" required>
      </div>
      <button type="submit" class="btn btn-primary">Guardar</button>
      <a href="/proveedores" class="btn btn-secondary">⬅️ Volver</a>
    </form>
  </div>
</body>
</html>
